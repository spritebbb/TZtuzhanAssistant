openai>=1.40.0
# core/llm.py 直接使用 httpx；不能只依赖 openai 的传递依赖（openai 3.x 已改用 httpx2）。
httpx>=0.27,<1
python-dotenv>=1.0.0
loguru>=0.7.0
fastapi>=0.110.0
uvicorn>=0.29.0
ddgs>=9.0.0
zhdate>=0.1
Pillow>=10.0.0
sqlite-vec>=0.1.9
python-multipart>=0.0.9
edge-tts>=7.2.0
# 知识库（D2 RAG）：PDF 文档解析
pypdf>=4.0.0
# 记忆系统 v2（重构新增）
chromadb>=1.5.0
sentence-transformers>=6.0.0
torch>=2.10.0
mem0ai>=2.0.0

# ---- 测试依赖（开发用）----
pytest>=8.0.0
pytest-asyncio>=0.24.0
