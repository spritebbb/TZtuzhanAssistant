# 菟菚助手全项目工作树（2026-09-20）

> 执行：ZCode（GLM），2026-09-20。应用户要求对全项目做一次完整梳理：已完成 / 未完成 / 需人工拍板三类全部列出。
> 核对方法：git 提交史（HEAD `833bfac`，2026-09-18）+ 未提交工作区实测盘点 + `docs/` 全部跟踪文档交叉核对（EXPERIENCE-UPGRADE-PLAN §4 状态回写表、RELEASE-QUALITY-GATE-2026-09-18、Zcode技术指导 §22–25、TAVERN-COMPANION、EVOLUTION-ROADMAP、AGENT-COORDINATION 信箱）+ 加密迁移 journal 实读。
> 本文档是**快照**；批次级动态状态仍以 EXPERIENCE-UPGRADE-PLAN §4 与发布质量门文档为准。

---

## 0. 项目快照（2026-09-20 实测）

| 项 | 状态 |
|---|---|
| 仓库位置 | `D:\TZtuzhanAssistant`（**已从 `D:\DSH\TZtuzhanAssistant` 迁移到盘根**；旧目录只剩 `.zcode` 空壳；根目录 M0 时代 `TODO.md` 已于 2026-09-20 归档至 `docs/archive/TODO-M0.md`） |
| HEAD | `72f3eb4`（2026-09-20，酒馆④在场程度旋钮）；v3.1.0 脏树已全部落库并打 tag |
| 远端差距 | **已推送同步**（2026-09-20）；`v3.1.0` 标签从 9f12929 重定位至发布内容终态 `e12735b` |
| 工作区 | 干净（v3.1.0 发布变更已按 8 片落库：见 §五） |
| schema | v42（tavern_sessions） |
| 发布物 | `frontend/release/菟菚桌面助手 Setup 3.1.0.exe`（142MB，SHA256 已固定）；v3.0.0 已发 GitHub Releases（09-08）；3.1.0 **已打 tag**、发布口径=**维持内测**（2026-09-20 拍板，质量门 Conditional Go） |
| 真实数据 | 已加密；明文副本与 `data-backup-20260913/` 明文备份均已删除（2026-09-18 / 09-20）；生产位已有验证过的加密备份（09-20 演练：备份+恢复行数一致） |
| 测试基线 | 2026-09-20 全量聚合 150 passed（14m46s，含新增成本闸门 5 组）；test_tavern 9 组；前端基线见质量门（175/115/15/5） |

---

## 一、已完成功能树

> 按能力域分组。每组末尾括号内为主要落点/提交线索。M0–M8 里程碑、M9 波次与 26 项缺陷、体验加量批次 1–16（含 Q1–Q5、流式改造）全部完成。

### 1. 对话与人格核心
- 流式对话（增量卫生流式 + 工具轮正文流式，批次 16A/B/C：`c0c783d`、`a799e0b`）+ 会话持久化 + 多会话 UI
- 工具循环（熔断 8 次/轮、同签名 2 次、32KB 预算、结构化错误）+ 天气/联网搜索/生图/识图（识图回落 Qwen3-VL-32B）
- 搜索双轨分工：中文博查优先、非中文 Tavily 优先（`ad7a83e`，实测数据齐全）
- TTS 朗读（edge-tts + 情绪声线韵律映射 + 自动播放开关）、多状态立绘（五档心情 × 四阶段距离）、自制表情包（低频 + 人设视觉锚定）
- 输出卫生（句号/括号旁白/思考段/AI 腔红线）+ 行为帧编译（`behavior.py`，理论术语不进 prompt）
- 人格 eval 护栏（B3 40 场景 + 确定性红线 + Q1 扩建为九组 fixture/盲评/发布门槛）
- 语言质地：低能量停顿、幽默强度、沉默倾向入 texture_line（M3 收尾）

### 2. 记忆系统
- 溯源事实（来源消息/置信度/验证时间）+ 冲突候选确认 + 记忆纠偏（删改 API + 管理页 + 对话内 LLM 仲裁真删）
- 显著度分级与冷热分层（G01：生产评分、期限消费、初历、视角注释、可见遗忘；`c57a99d`、`475f71d`、`371c98d`）
- 自然衰减（retention_days + 到期统一生命周期）+ 长期保留 pinned
- MEMORY_V2（BGE-M3 1024 维 + v1 融合兜底，真实数据 866/868 验证）
- 临时对话「本轮不留痕」（插件钩子全禁 + 零副作用建档）
- 记忆生命周期对话内露出（F07）、心事门控（F03）

### 3. 关系系统（能力评估 9.5/10，护城河）
- 双维门槛 min(trust,intimacy) 跨 25/50/75 + 阶段内小档 + 关系气质（L03 五气质 + 形成原因可解释）
- 关系事件内核（类型注册/幂等/过期/纠正/级联）+ 链式反应最小引擎（P2-04）+ 对称约定（P2-03）
- 追发/主动收尾/称呼候选/晚安仪式（P2-05，`conversation_rhythm.py`）+ 重逢三段式（P2-06，M8 收官）
- 30/100/365 天关系快照、不同版本的我们（确定性比较不判好坏）、双视角叙事、全活动感想栏
- 五域领域信任（L05）、幽默记忆与梗（L04）、冲突→修复多轮（张力上限/道歉才降）
- 关系导出/恢复（9 类别 + kv 登记表 + 主键重映射）、阶段封存纪念包（只导出不删除 + LLM 告别信）
- 好感度玩法闭环（6 阈值解锁 + 3 彩蛋）、养成仪表盘（7/30/90 天聚合 + 原生 SVG 曲线）

### 4. 主动性与她的生活
- 统一主动仲裁器（勿扰/专注静默/去重/原子占位/共享额度/失败冷却，全部外置可调）
- 表达必要性门 + 注意力漂移（§17.1，已激活进主链路）+ 可审核学习管线（§17.2）
- 未完成心事 + Narrative Planner（阶段门控 + 每日一条）+ 悬念/开放问题（G03 全状态机）+ 她开口求助与欲望（G04 双门槛）
- 行程与每小时 tick（P1-04）+ presence 展示 + 外出状态一致性（`c4cbae8`）+ 日历调制（P1-05）+ 纪念日预谋（前夜心不在焉）
- 生活模板池与精力有限选择（L06，`c4856eb`）+ 休息沉默与连续消息唤醒（`3d451e2`）+ 睡眠门（`sleep_gate.py`）
- 惊喜编排（真实素材 + 低频三重门，批次 13 放宽为 7 天/50%/120min）+ 问候变体池（F01 四类）
- 离线叙事（梦境/研究/生活碎片，初识降级）+ 场景化表达观察复活（`user_style_map`，`3ac4bd2`）
- 主动发图（四层管道 image 字段）+ 先做事再汇报（备料后提醒）

### 5. 共同活动
- 共读 2.0（分段进度/书签/双角色观点/共同书摘/45 天回访）+ 阅读地图与书签解锁（F05）
- 专注陪伴（状态机/安静模式/惰性结算/克制复盘）+ 专注收尾人格化投递箱（F04）
- 共同目标（真实进展/到点提醒不催债）、共同创作轮流续写（虚构隔离）、共同清单（歌单/书单）
- 网页正文/EPUB 摄入（L01：URL 边界/防炸/分段/去重/任务取消 + 前端入口）
- 世界观/角色设定共创 + 观察日志（L02）+ 大纲 subtype
- 意图自动预填立项（F06）

### 6. 知识与语境
- RAG 知识库（pdf/txt/md + 分块 + kb 向量分区 + 距离门控 + 「她的书架」）+ 知识观点（形成/撤销/进对话）
- 语境注册表（P1-02 迁移 + 开关接入 + 观点 provider 激活）
- 世界正典与侧写（P1-01 九节内容初稿全部拍板并入）+ 正典→日程素材桶映射
- 共享知识授权（L16：shared_tables + 只读授权 + 导出 ACL）
- L08 统一 VisualState/Presence + 工具臃肿治理

### 7. Agent 与工具（能力评估 7.5/10）
- 标准 MCP 客户端（Streamable HTTP + SSE 回退）+ stdio 桥 + 按需注入 + Context7（HTTPS pinning 崩溃已修）
- 聊天派活、定时调度、失败重试、产物落盘、待办衔接（七项缺口全补）+ 监控 Agent（watches 表 + 内容哈希不存正文）
- 任务面板（重试/定时按钮）+ 子代理并行（agent_fanout）+ 技能通道修复（沉睡功能激活）
- 内置能力演示教程（18 步按能力域分组 + 前置条件实时探测）+ 演示模式
- 聊天外链系统浏览器打开 + 链接协议白名单

### 8. 隐私与安全（能力评估 9.0/10）
- P3-04 A–E 全链：数据面 ADR + SQLCipher/分块 AEAD + central connector 收口 + 密钥层级（MK/DPAPI/Argon2id/key broker）+ 应用锁 + 迁移引擎（状态机/原子切换/CLI `--migrate/--cleanup`）+ 锁定启动惰性开库 + 一键启用加密
- **真实 `data/` 已于 2026-09-13 迁移到加密目录，明文副本 2026-09-18 已删**（journal `plaintext_removed_at` 在案）
- 临时轮零落痕、默认拒绝确认闸门、危险命令黑名单、路径白名单、全程审计
- 提示注入矩阵八类入口 + 资源授权 + 密钥脱敏（Q2）；request_id 全链路遥测（Q3，可关可清）

### 9. 质量与工程底座
- Q1–Q5 全绿：评测框架/注入矩阵/可观测性/性能预算与旧库迁移/无障碍与克制界面（对话框焦点、键盘闭环、对比度、200% 缩放、360px 窄屏、reduced-motion E2E）
- GitHub Actions CI（regression.yml，后端+前端两 job，已与本地同构验证）——**文件在未跟踪区，随脏树待提交**
- 全项目死代码审计（§23：Vulture + Knip + rg 复核，测试套件数据目录隔离修复）
- schema 版本守卫（v13→v42 全程 test_schema_backup 联动）+ 单主题提交纪律 + kv 登记表
- 每日批处理 off-by-one 等真实 bug 修复史（diary 空表根因、时间漂移测试、GCM 推送挂起）

### 10. 交付形态
- Electron 打包：v2.0.0 → v2.1.0 → v3.0.0（GitHub Releases）→ **v3.1.0 安装包已构建**（Electron 44 + 后端随包分发自举启动链 `bb5df0a` + NSIS + 泄露扫描 0 命中）
- PWA 基础 + 局域网/远程 token + 公网部署选型文档（Tailscale/Cloudflare/Caddy）
- 部署：start.bat 整合 MCP 桥、迁移 CLI、备份清单

### 11. 酒馆同玩（SillyTavern 同伴扩展）
- `/tuzhan` 点名上桌、`@菟菚` 静默频道（拦卡生成）、自动插话（概率+冷却+她可沉默）、世界书带入（checkWorldInfo）
- 收局沉淀真记忆（LLM 忠实摘要 → long_memory + 向量 + 剧情原文落 tavern_sessions，门控注入）
- 身份锚定防人格滑走、不可信包裹防世界书注入、串演边界、字数上限全拆（用户拍板）、开关标签修复
- 2026-09-18 收尾：reset 双清单 + 关系包导出恢复 + 文本主键重分配，提交 `62d8d36`

---

## 二、未完成

### A. 在途：v3.1.0 发布变更（代码已在工作区、验证全绿、**未提交**）

质量门 2026-09-18 实测后留下的脏树，内容 = 本轮变更面：
- **turn 管线拆分重构**：`backend/core/turn_context_gather.py` / `turn_effects.py` / `turn_prompt.py`（新）+ `pipeline.py` 接线
- **effect ledger 与降级计数**：`backend/core/effect_ledger.py`（新）+ `api/meta.py` 暴露 effect_stats
- **P3-04 F 片加密备份引擎**：`backend/maintenance/encrypted_backup.py`（新）+ `backup_manifest.py`/`loop.py`/`scripts/restore_backup.py` + `tests/test_encrypted_backup.py` —— 代码与自动化测试已就位，**真实加密备份/恢复演练未执行**（明文周期备份已按 ADR 停用等它接管）
- **CI**：`.github/workflows/regression.yml`（未跟踪）
- **Electron 44 升级 + 桌面外壳冒烟**：`frontend/package.json` + `frontend/scripts/`
- 其他：`storage/migration.py`/`runtime.py`、`agent/session.py`、`initiative.py`、`tests/conftest.py`、README/ARCHITECTURE/.gitignore、`tests/test_effect_ledger.py`
- 待办动作：选择性提交 → 打 tag → 从 tag 重建安装包并复算哈希（公开发布前置条件，见 §三 P-01/P-02）

### B. 已立项/有完整方案，未开工（批次 15 D 类离线件 + 设计项）

| 项 | 内容 | 前置条件 |
|---|---|---|
| P3-03 | 本地 GPT-SoVITS TTS（adapter 协商/缓存/回退，不训练） | GPU/音色素材；2026-09-08 曾拍板暂缓 |
| L09 | 本地 Whisper STT worker 骨架（framed 协议，不装模型） | 无硬前置，纯离线件 |
| L10 | 桌面宠物 petWindow 骨架（IPC 白名单/点击穿透，mock 测试） | 依赖 L08（已完成） |
| L12 | D8 公网 HTTPS + Access + PWA Web Push | **域名/Cloudflare Access tenant（用户条件）** |
| L13 | QQ 官方机器人首接（channel bus/inbox/outbox/一次性码） | **QQ 官方账号获批权限** |
| L14 | 个人微信本地桥接（wechat sidecar 协议） | **微信客户端与 bridge 兼容版本** |
| L15 | 多角色群聊与各自生活 | 实施时序延期（拍板过） |
| D9 | 局势档案（≤2.5k tokens 恒定世界快照，ZZZChats 可搬） | 仅设计（2026-09-13 增补） |
| D10 | 成本护栏叙事化熔断（按任务记账 + 叙事化限流） | 依赖 D5 记账（已完成），待立项 |
| D11 | 离线补算管线（重放日程/限额裁剪/排队回放） | 依赖 P1-04（已完成），待立项 |
| D12 | 主动意愿 roll（p=性格+好感−压抑 的统一打分器） | 建议 B3 eval 护栏先行（已具备） |
| ST 同玩 ④ | 18+ 在场程度三档旋钮（唯一剩余切片） | 等 §三 P-07 拍板后一起做 |
| L11 余量 | 世界感知来源层：网页/EPUB/RSS 摄入边界已有（`external_content.py`），主题订阅/节日的产品形态未单独立项 | 待需求确认 |

### C. 工程债与技术建议（§24.3，能力评估后列出、未排期）

1. **成本闸门**：子代理并发上限、单任务 token 预算、嵌套深度限制（`agent_fanout` 当前无上限）
2. **可观测性**：任务执行轨迹（每步工具/耗时/失败原因）进面板，替代粗日志
3. **权限中间态**：「这类操作限时免确认」（介于逐次点击与全放行之间）
4. **端到端回归**：真模型 + 真工具链冒烟集（聊天派活→计划确认→工具执行→产物落盘）

### D. 发布质量门未覆盖项（公开发布前必须补，多数需真人执行）

- 真机 UAT（完整聊天/主动性/记忆/人格切换/锁定解锁）、真实 LLM 体验验收（中文质量/长期上下文/人格稳定性）
- Win10/Win11 兼容矩阵（DPI/多显示器/休眠唤醒/托盘通知/无 Python 环境首装）
- 性能与容量（冷启动/长会话/大量记忆/真实 embedding 的 P50/P95/P99、RSS、失败率——无负载模型与阈值）
- 发布级数据恢复/回滚演练（真实加密备份创建、恢复、密钥丢失恢复）
- 生产监控告警与值班链路、隐私政策与法律合规签字
- 文档债：~~根目录 `TODO.md` 严重过时~~（**2026-09-20 已归档**至 `docs/archive/TODO-M0.md`）；`D:\DSH\TZtuzhanAssistant` 旧空壳目录残留

---

## 三、需用户人工拍板

| # | 决策 | 背景与选项 | 影响 |
|---|---|---|---|
| P-01 | **v3.1.0 公开发布 Go/No-Go + 代码签名** | 质量门结论 Conditional Go（仅内测）。公开发布最低条件：①正式 Authenticode 签名，或不签但具名书面接受 SmartScreen/供应链风险；②UAT/真机/恢复演练具名签收；③ChromaDB 风险具名接受；④回滚决策人/监控责任人指定 | 不拍板则保持内测，安装包不能公开分发 |
| P-02 | **脏树提交 + 打 tag + 从 tag 重建** | 46 个未推送提交之上还有整套 v3.1.0 变更未提交，制品来自 dirty tree 不可追溯。需确认提交拆分方式（建议按质量门变更面分片提交，遵循单主题纪律） | 公开发布的硬前置；也是当前最大的一笔「做了但没落账」 |
| P-03 | **推送 46+ 提交到 GitHub** | 本地领先 `origin/main`（e1beeda）46 个提交。推送命令需带 `-c credential.username=spritebbb`（GCM 弹窗坑已实证） | 备份与协作安全；不推则单点 |
| P-04 | **`data-backup-20260913/` 明文备份处置** | 加密迁移时 `removed_paths` 只清了 `data.plaintext-*` 与 `premigration-backup`；仓库根这份 09-13 备份**仍含明文 bot.db/sessions.db/agent_tasks.db**（另有更早 .bak 文件）。选项：确认加密库可用后删除 / 迁入加密备份（F 片演练正好可用它做素材） | 隐私风险敞口；建议与 P-06 恢复演练合并处理 |
| P-05 | **ChromaDB 4 条无修复安全公告的风险接受** | pip-audit 命中 4 个唯一公告均无 fix 版本；当前仅本地 PersistentClient、未暴露 HTTP Server，架构上判定「不适用」。需要具名签字接受，或决定升级/替换 | 公开发布前置；引入 Chroma Server 前必须重评 |
| P-06 | **真机验收与恢复演练执行人** | UAT、Win10/11 矩阵、真实 LLM 体验、真实加密备份恢复——质量门明确「待人工验收」，自动化无法替代 | 公开发布前置；可与 P-04 合并做一轮 |
| P-07 | **ST 18+ 本地模型（Ollama）装/不装** | 2026-09-14 提出：RTX 4060 Ti + 32GB 可跑 8B 流畅/14B 可行；装=本地无风控，用户自行处置人格卡；不装=现状收束（实质段落她淡出）。用户尚未答复；④在场程度旋钮可一并做 | 决定酒馆同玩的最终形态 |
| P-08 | **D 类外部条件何时启动**（L12 域名/L13 QQ 账号/L14 微信 bridge/L09-L10 纯离线件/P3-03 GPU+是否解冻） | §22.2 允许的「开工前未知」全部属于此类；离线骨架（L09/L10）无外部条件可先做 | 决定下一波排期方向：对外渠道 vs 桌面形态 vs 语音 |
| P-09 | **§24.3 四项工程债的排期**（成本闸门/执行轨迹/权限中间态/端到端回归） | 评估结论：拖后腿的不是功能数量而是成本闸门与可观测性 | 长期使用成本与安全 |
| P-10 | **D9–D12 四项设计是否立项**（局势档案/叙事化熔断/离线补算/意愿 roll） | 2026-09-13 增补进路线图，均可从既有底座点火 | 人格连续感的下一档升级 |

---

## 四、核对说明与已知环境问题

- 本次梳理时本机 Git Bash（`D:\Git\bin\bash.exe`）已失效（ENOENT），ZCode 的 shell 工具不可用；侦查改经只读命令通道（git/findstr/cmd dir）完成，未改动仓库任何既有文件（仅新增本文档）。
- `TODO.md`（根目录）为 M0 时代遗物——**2026-09-20 已归档**到 `docs/archive/TODO-M0.md` 并从根目录移除。
- 信箱（AGENT-COORDINATION.md）最后一条汇报停在 2026-09-12；09-13 之后的酒馆收尾与发布质量门工作未走信箱（TAVERN-COMPANION 注明由「菟菚助手工作区会话」执行），本文档以 git 与 docs 实物为准核对。

---

## 五、2026-09-20 拍板结果与执行回写（执行：ZCode（GLM））

**十项拍板结果**：P-01 维持内测（不签名不公开，真机验收后再议）；P-02+P-03 分片提交+tag+推送 ✅；P-04 演练后删除 ✅；P-05 接受 ChromaDB 风险（本地 PersistentClient 不适用论，引入 Server 前重评）；P-06 待用户执行（清单已备 `docs/UAT-CHECKLIST-2026-09-20.md`）；P-07 不装 Ollama（现状收束，④旋钮照做）；P-08/09/10 下一波顺序=成本闸门→L09+L10→D9-D12→L12-L14。

**已执行**：

1. TODO.md 归档（`docs/archive/TODO-M0.md`）。
2. v3.1.0 脏树八片落库：`8946c02` initiative_loop 导入修复 / `f8bbaee` conftest 测试隔离 / `a18f2f6` turn 拆分+effect ledger / `c6f256b` P3-04 加密收口+F 片 / `81d8c73` Electron 44+冒烟 / `e57cb0e` CI / `8627044` 质量门文档 / `e12735b` 工作树+归档；`v3.1.0` 标签重定位至 `e12735b`；main 与标签均已推送。
3. P-04 全链：DPAPI 解锁校验 → 生产位加密备份（`encrypted-periodic-20260920-234812`，14 文件）→ 临时目录恢复演练逐库校验（行数一致）→ 删除 `data-backup-20260913/` 明文备份。
4. 成本闸门（§24.3-1，`eed6787`）：llm 任务级计量 + 子代理全局并发信号量（AGENT_SUBAGENT_CONCURRENCY=3）+ 单任务 token 预算熔断（AGENT_TASK_TOKEN_BUDGET=120000，贯穿工具循环两路与子代理预检）+ 嵌套深度闸（AGENT_SUBAGENT_MAX_DEPTH=1）+ fanout 上限外置（AGENT_FANOUT_MAX=5）；`tests/test_agent_cost_gates.py` 5 组。
5. 酒馆④在场程度三档旋钮（`72f3eb4`）：`presence` full/moderate/shy，后端场景指令 + API Literal 校验 + 扩展侧设置面板（ST 扩展在 `D:\SillyTraven` 仓库外同步修改）；`test_tavern` 增至 9 组。
6. 验证：全量聚合 150 passed（14m46s）。

**§二清单状态更新**：A 在途→已清零；B 表「ST 同玩 ④」→已完成；C 工程债第 1 项（成本闸门）→已完成，第 2-4 项（执行轨迹/权限中间态/端到端回归）仍开放；D 的明文备份条目→已处置。

**下一波（用户已定序）**：L09 STT worker 骨架 + L10 桌宠骨架 → D9-D12 立项设计 → L12-L14 离线件（域名/QQ 账号/微信 bridge 等外部条件留空位）。

---

## 六、2026-09-21 增补：L09/L10 桌面骨架落库（执行：ZCode（GLM））

- **L09 本地 STT worker 骨架**（`4275976`）：帧协议/双上限/取消复位/模型白名单/无模型确定性报错，8 组回归；不装模型、真机语料留空位。前端接线（`09700dd`）：Electron 宿主（无 shell、IPC 四通道白名单）、ChatInput 麦克风（权限失败不建请求、final 只回填草稿不自动发送、PWA 禁用态）、`.env.example` 增 `LOCAL_STT_*`。**模型下载 UI 未做**——首次使用需手动把 tiny/base/small 模型目录放进 `<数据目录>/local_stt_models/`。
- **L10 桌宠骨架**（`09700dd`）：`src/desktop/` 纯逻辑（clamp/DIP/避让决策/单实例清理）+ `electron/petWindow.ts`（透明 frameless、contextIsolation、showInactive、IPC 白名单、屏幕事件重 clamp、偏好落盘）+ `PetView.vue`（复用五档立绘、Escape 关闭、reduced-motion）+ `pet.html` 独立入口 + 设置页开关。**前台全屏 helper 未实装**（避让=mock 模式不隐藏）；多屏/DPI/8 小时真机验收留空位。
- **验证**：Vitest 138/138、vue-tsc+build、Electron 冒烟 5/5、worker 8 组。批次15 状态表已回写。
- **§二B 表更新**：L09/L10 行从「未开工」改为「骨架已落地（真机/模型/避让 helper 留空位）」；P3-04 F 片补记 c6f256b + 09-20 真实演练全绿。
- **剩余排位**：~~D9-D12 立项设计~~（**2026-09-21 完成并拍板**：`docs/D9-D12-DESIGN-2026-09-21.md`，实施序 D12→D10→D9→D11；月预算 ¥30 三档 70/100/130；档案事件驱动+每10轮；回放逐条可跳过）→ **片2 D10 熔断 → 片3-4 D9 局势档案 → 片5 D11 离线补算** → L12-L14 离线件。
- **D12 意愿 roll 已落库**（`c8cd657`）：necessity 达标后掷「她此刻想不想说」，可复现种子+想念补偿，约定到点硬规则免骰，开关 `willingness_enabled` 进设置页；6 组新回归 + 受影响套件全绿。
- **D10 叙事化熔断已落库**（`0b078cb`，2026-09-21）：cost_guard 四档（¥30×70/100/130）——soft 主动更挑剔+每日一次「最近事多」轻提；hard 自主 LLM 全停（主循环/拉模式/仲裁器/daily 批处理，journal 留痕不产半成品）；extreme 路由降级+常驻简短质地。**用户聊天任何档位不受影响**；叙事文案无数字无系统词。8 组新回归 + 11 套件复跑全绿。§22.1 的「实测 ¥30 上限」决策由此接线成体验。
- **D9 局势档案已落库**（`067e338`，2026-09-21）：schema v43 `situation_files` 每人格一行恒定世界快照（六节：目标/约定/悬念/近事件+压缩段/生活/焦点），SQL 确定性重建 + LLM 仅压缩更早事件（失败保旧）；turn 副作用后台增量（事件驱动+每10轮，D10 门控）；daily 尾部零成本重编译；常驻 system 注入（gather_all 首位，人格后记忆前）；`situation_enabled` 开关 + `/api/situation` 诊断。8 组新回归 + 9 套件复跑全绿。**效果：进行中的目标、没兑现的约定、没解开的疑问从此每轮恒定在场，不靠检索命中。**
- **D11 离线补算已落库**（`8fb6cc7`，2026-09-21，D 系收官片）：确定性重放真实行程引擎（离线时间戳/睡眠跳过/同块去重/>48h 每日一采样）+ 限额裁剪（8 条上限，超出降级摘要行）+ 仅开场一句 LLM（素材含 D9 档案，D10 hard 零 LLM，失败回落确定性开场）；kv 状态幂等（offline:pending/last_recap）；app 启动预生成 + GET 懒生成兜底 + POST ack；ChatView 逐条回放（时间戳前缀+跳过横幅）。6 组新回归。**自行收敛两处（提交已注明）：不新增表（kv 承载）、不写 character_life_events 派生行。**
- **D9-D12 全部落地**（五片：D12 `c8cd657` → D10 `0b078cb` → D9 `067e338` → D11 `8fb6cc7`），节点三全量聚合见 §七。

## 七、节点三（2026-09-21）：D 系收官全量聚合

**后端全量 155 passed / 0 failed（913s，`pytest tests/test_suite_runner.py`）**——含五个新套件（stt_worker 8 组 / willingness_roll 6 组 / cost_guard 8 组 / situation 8 组 / offline_recap 6 组）。前端 vitest 138/138、vue-tsc+build、Electron 冒烟 5/5（片内验证）。

首轮全量曾出 5 红，全部根因清零并修复：
1. **ephemeral 真 bug**（`02fba3f`）：局势档案在「本轮不留痕」时也会落盘派生档案——situation_context 加 persist 开关，临时轮只读渲染；
2. **骰子假红 ×3**（`02fba3f`）：agent_dispatch/watchers/companion_requests 三个「断言投递成功」套件被 D12 按分钟播种的意愿骰子随机击穿，补关骰前置（与 arbiter/surprise 同修法）；
3. **迁移切换句柄竞态**（`112074a`，既有债务非 D 系引入）：一键启用在关库后仍有一条惰性 sqlite 连接持有源库，切换 rename 全数 PermissionError 且旧回滚报误导性 FileNotFoundError——引擎切换前终局清场（关全部库+GC 扫尾关残余连接）+ 指数退避重试（覆盖杀软扫描窗）+ 回滚容错上抛真实主错误。encryption_enable 复测 9/10（残余偶发为杀软瞬时持柄，停机操作可重试）。

**D 系四件全部上线后的新体验**：值得说的主动消息她会再想想想不想说（连续没说会想念补偿）；月开销到档她按日程去忙（你的聊天永不受影响）；进行中的目标/约定/悬念每轮恒定在场；你不在的日子她照常过，回来逐条讲给你（可跳过）。
