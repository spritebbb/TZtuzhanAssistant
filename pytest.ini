[pytest]
; 现有测试是「脚本 + main() 编排」风格：只收集测试套件入口和无副作用的
; 边界回归用例。其余 test_*.py 会在导入时设置环境变量或初始化全局状态，
; 若让 pytest 在收集阶段导入它们，会污染 suite runner 及其后续子进程。
python_files = test_suite_runner.py test_edge_regressions.py
python_functions = suite_*
asyncio_mode = auto
