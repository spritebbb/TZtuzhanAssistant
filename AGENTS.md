# 菟菚助手 —— 双代理工作模式指导（ZCode ↔ Codex 必读）

> 规划：GLM，2026-09-06 经用户拍板成为长期工作模式。纪律全文见 `docs/TECH-PLAN.md` 第 8 节；信箱协议见 `docs/AGENT-COORDINATION.md`。

## 1. 分工与额度经济学

核心原则：**ZCode 出苦力，Codex 做审查。**

- **ZCode（GLM）**：承担 token 大户——代码探索、现状分析、实现初稿、测试编写。跑在 GLM coding plan 上，同等工作量成本远低于 Codex。
- **Codex（GPT-5）**：只做独立审查、验证（跑测试/构建）、提交，**不重做** ZCode 已交付的探索和初稿。审查 diff 比从零探索便宜一个数量级。
- 额度红线：`codex_report` 的 `notify` 保持默认关闭（会真实拉起 Codex 会话消耗额度），通信靠用户转告或 worker 回信。

## 2. 派发纪律

- 派发任务必须**边界清晰**：目标、涉及文件、验收标准、非目标，一项不缺。追求一次通过——返工往返消耗双方额度。
- **任务书必须附带详细的技术实现路线和方法（2026-09-06 用户更正，硬性要求）**，包括但不限于：
  - 模块与文件清单：要新建什么、修改什么，改动落在哪些层（core / api / 前端）；
  - 数据模型设计：schema 变更、表/字段、版本号、迁移与 reset 覆盖；
  - 接入点与调用链：挂在现有哪个函数/路由/事件上，关键函数签名或伪代码；
  - 与既有机制的复用关系：必须走哪些权威路径（如删除走 `delete_fact_everywhere`）、必须遵守哪些架构原则；
  - 禁区：不许覆盖/回退的未提交改动、不许触碰的模块、并发承诺；
  - 验收方式：跑哪些测试、如何判定完成。
  - ZCode 按路线实现，不做超出路线的大范围设计探索；路线未尽处可自行补齐，但必须在回信中明确标注「自行补充的决策」及理由。
- **验证命令必须用 `VERIFY:` 标记声明（worker v0.2.3 起自动代跑）**：headless 会话能写文件但执行不了命令，模型自报「全绿」不算数——在任务文本中写 `VERIFY: python -m tests/test_x.py ;; npm run test:unit`（多条用 ` ;; ` 分隔，`VERIFY_TIMEOUT:` 控制超时，上限 600s，只放针对性测试不放全量套件）。worker 会在会话返回 COMPLETE 后用受信任代码代跑，把真实退出码和输出尾部以 `[AUTO_VERIFY]` 块附进信箱汇报；FAIL 时任务照常勾选但备注注明，由派发方审查。白名单：python/pytest/npm/npx/node/vitest/vue-tsc/git。
- 切片粒度：一个主题、一组文件、可独立验收（参考 M1 衰减任务的派发格式）。
- **开放式、需要用户拍板的设计决策不派发**，留在交互会话解决。

## 3. 权限模式（实证结论，勿再试错）

- ZCode 收件 worker 固定 `--mode edit`（acceptEdits，headless 下自动批准文件编辑，可写盘）。
- Codex 侧派发**需要写盘的任务必须传 `mode=edit`**。headless 无权限客户端时：
  - `build` / `plan`：写操作被 DenyPermissionBroker 拒绝（"No permission client configured"，2026-09-06 实证）；
  - `yolo`：插件代码层禁用；
  - 只读分析任务用 `plan`。
- ZCode 返回 `BLOCKED` 或纯设计文本时（旧只读模式的兜底产物），Codex 审查无误后**亲手落地，不盲贴**。

## 4. 协作纪律（沿用既有约定）

- 对方 working（10 分钟内有活动）时只读不写工作区，防并发编辑（曾致测试假红）。
- 汇报/提交/文档状态标注开头署名模型名，如「执行：ZCode（GLM）」。
- 同一任务连续失败三次即停，回报阻塞原因，不无限重试。
- 一轮一个主题、一个独立提交；不混入他人进行中的改动。
