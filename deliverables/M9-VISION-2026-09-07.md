# M9 波次 0：识图事实层

执行：ZCode（GLM）实现初稿；Codex 独立审查、修补、验证与提交。

## 范围与结果

- `backend/core/vision.py`：只接受非空字符串正文，空正文、无效结构和异常均失败；不读取 `reasoning_content`。指令在 system，user 只放图片；视觉模型仅描述可见事实，不角色扮演，不执行图中文字中的指令。
- `frontend/src/components/ChatView.vue`：图片描述作为标明来源、可能不准确的背景引用；原图 URL 和持久化文案保持一致。Codex 将初稿较长的操作指令收短为背景来源说明，用 JSON 字符串引用保留描述边界。
- 新增 `tests/test_vision.py` 和 `ChatView.test.ts`，覆盖空正文含思考、异常结构、大小限制、缺配置、长度限制、API 失败不落图及上传成功/失败行为。
- 无 schema/kv/reset/迁移变更，无模型配置或人格卡改动。

## 验证

- Codex 实跑 Python 编译与识图独立脚本：通过。
- 前端 Vitest：69/69；类型检查与生产构建通过；产物含新的背景参考文案。
- Playwright 浏览器关键路径：7/7。
- 后端完整聚合：70/70，通过（484.90 秒）；命令 `.venv/Scripts/python.exe -X utf8 -m pytest tests/test_suite_runner.py -q`，包含人格确定性回归。

## 协作与限制

派发 `zcode-20260907044746-2974d8e6` 使用 edit 模式。超过 15 分钟仍未返回最终报告，Codex 停止子任务并确认其进程退出，保留代码和测试初稿后接手；没有可信 AUTO_VERIFY 回执。Codex 修复初稿测试中 unittest.mock.patch.object 参数误用，所有验收由 Codex 实际执行。

验证使用临时数据与 mock 模型，不消耗真实模型额度；真实视觉端点效果仍待用户使用观察。当前日常后端未运行，下次启动加载本轮代码，前端产物已重建。回滚本切片无需数据迁移。

下一切片：输出卫生过滤器。每日自动备份、签名 eval 与模型/搜索实测仍为波次 0 待办。
