# M1.2 MEMORY_V2 验证报告

日期：2026-09-06

## 结论

当前不需要重构记忆系统，也不需要重建向量库。保持 MEMORY_V2 默认开启，并保留 v1 词面召回作为融合兜底。

## 只读验证结果

- SQLite→Chroma 逐 ID 完整性：866/866，缺失 0。
- 向量维度：长期记忆、事实、三元组、画像和日期分区均为 1024 维 BGE-M3。
- 匿名历史自检索：长期记忆 20/20、事实 20/20；生产融合结果与 v1 都是 100% Top-3。
- 合成释义对比：BGE-M3 5/6，字符二元组词面检索 0/6。
- 模型冷启动：约 18.8 秒。
- 模型工作集增量：约 661 MB。
- 热查询：约 49–84 ms。

## 固化门槛

`scripts/validate_memory_v2.py` 在以下任一情况返回失败：索引不完整、未加载真实模型、冷启动超过 60 秒、工作集增量超过 1 GB、热查询超过 300 ms、融合历史召回劣于 v1、合成语义命中低于 75% 或不优于词面检索。

验证过程不写数据库、不重建索引、不打印或上传真实记忆内容。

后端聚合回归：53/53 通过。
